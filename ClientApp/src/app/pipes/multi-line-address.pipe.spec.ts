import { MultiLineAddressPipe } from './multi-line-address.pipe';

describe('MultiLineAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiLineAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
