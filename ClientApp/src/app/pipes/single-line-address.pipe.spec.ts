import { SingleLineAddressPipe } from './single-line-address.pipe';

describe('SingleLineAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new SingleLineAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
