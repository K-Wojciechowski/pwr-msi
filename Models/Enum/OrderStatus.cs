namespace pwr_msi.Models.Enum {
    public enum OrderStatus {
        CREATED,
        PAID,
        ACCEPTED,
        REJECTED,
        PREPARED,
        DELIVERED,
        COMPLETED,
        CANCELLED
    }
}
