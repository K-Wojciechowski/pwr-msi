namespace pwr_msi.Models.Enum {
    public enum OrderStatus {
        CREATED,
        PAID,
        DECIDED,
        ACCEPTED,
        REJECTED,
        PREPARED,
        DELIVERED,
        COMPLETED,
        CANCELLED
    }
}
